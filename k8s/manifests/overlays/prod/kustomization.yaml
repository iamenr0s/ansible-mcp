apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ansible-mcp

resources:
  - ../../base

commonLabels:
  environment: prod

patches:
  - path: patch-resources.yaml

images:
  - name: your-registry/ansible-mcp
    newTag: "1.0.0"
