apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ansible-mcp-workspace
  namespace: ansible-mcp
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard
