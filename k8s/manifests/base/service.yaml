apiVersion: v1
kind: Service
metadata:
  name: ansible-mcp
  namespace: ansible-mcp
spec:
  selector:
    app: ansible-mcp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
